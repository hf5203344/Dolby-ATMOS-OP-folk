# Dolby Atmos KSU 模块 — 设计审查与改进报告

> **审查对象:** `/workspace/设计/` 6 份详细设计文档 (M1-M6) + INDEX + progress.md + prompt.md
> **审查日期:** 2026-07-02
> **审查方法:** 问题驱动 + 横向一致性 (详见 [审查计划](.trae/documents/dolby-atmos-design-review-plan.md))
> **审查范围:** 仅文档, 不包含实现代码
> **严重度:** Critical (C) / High (H) / Medium (M) / Low (L)

---

## 1. 总体评估

### 1.1 项目成熟度评分: **6.5 / 10**

| 维度 | 评分 | 说明 |
|------|------|------|
| 完整性 | 8/10 | 6 个模块职责清晰, 接口定义明确 |
| 一致性 | 5/10 | 跨模块存在多处 UUID/枚举值不一致 |
| 正确性 | 6/10 | 关键路径 (M3 音效链) 有逻辑漏洞 |
| 可测试性 | 6/10 | 多数测试需要真实设备, 缺乏可编程 mock |
| 可维护性 | 5/10 | 缺少构建系统、版本管理、单一事实源 |
| 文档质量 | 8/10 | 设计文档本身详尽, 但缺少故障排查手册 |

### 1.2 关键路径风险

```
M1 (框架) ─→ M2 (解码器) ─→ M3 (音效链) ─→ M4 (APK)
                          ↓
                       M6 (兼容)
```

**最大风险点: M3 音效链**

- 文档明确说 "音效不可用的根因经分析, 最可能的原因是 HIDL 音频 HAL 与 AIDL 音频 HAL 的兼容性问题", 但 **没有提供诊断这个兼容问题的实际方法**。
- 主方案 + 附录 A 备选方案并存, 但缺少 "何时切换到备选方案" 的判定标准。
- `merge_audio_effects` awk 脚本的状态机对 XML 格式假设过强, 真实设备上 XML 可能因换行/缩进差异导致合并失败。

### 1.3 整体印象

这是一个**设计阶段成熟、实现细节粗糙**的工程:

- 顶层架构和模块切分做得不错, 6 个模块边界清晰、依赖方向合理。
- 落到具体代码时, 大量细节被当作 "实现期再说" 标记为 "待确认" / "TBD", 缺乏"现在就定下"的魄力。
- 测试方案普遍是 "在真机上验证", 没有设计可编程的单元测试和 CI 流水线。
- 跨模块的常量 (UUID、路径) 没有单一事实源, 靠文档传播, 一旦修改容易漏改。

---

## 2. 优点清单 (值得保留的设计决策)

1. **模块切分清晰**: 6 个模块按职责划分 (框架/解码/音效/APK/诊断/兼容), 依赖关系明确。
2. **3 种 Root 方案统一兼容**: customize.sh 中 detect_root_solution() 同时处理 KSU/Magisk/APatch, 不强制要求特定 Root。
3. **AudioEffect + DMS 双路径**: M3 附录 A 的备选方案设计, 在 AIDL 不兼容时可降级到纯 AudioEffect 方案, 体现了"留后路"的工程思维。
4. **设备参数化**: M6 的 `get_device_dap_params()` 按 PKG110/120/130 等机型返回不同参数, 便于扩展。
5. **诊断 10 阶段设计**: M5 的 action.sh 把诊断过程结构化, 输出可分享的报告, 实用性强。
6. **运行时挂载而非文件覆盖**: service.sh 用 `mount --bind` 替换 VINTF manifest 和 AudioX feature XML, 不动原厂分区, 卸载干净。
7. **状态机可视化**: M4 文档中的效果状态机 + DMS 连接状态机, 帮助理解复杂流程。

---

## 3. 问题清单 (按严重度)

### 3.1 Critical (4 条) — 必须在编码前修复

| # | 模块 | 问题 |
|---|------|------|
| C-1 | M1 + M6 | `detect_root_solution()` 在 M1 和 M6 中各定义一次, 行为不一致: M1 写 `ROOT_TYPE` 全局变量, M6 `echo "KSU"`. M1 source 引入 M6 后, 两个同名函数将冲突。|
| C-2 | M2 | 6 个 `.so` 文件的 SHA256 全部标注 "**待确认**", 但 VINTF 校验、文件完整性、防篡改都依赖这些值。无法在编码前完成 SHA256 校验, 整个 Phase 4 集成测试无法设计。|
| C-3 | M4 | **M4 文档明确说"手动依赖注入, 避免引入 Hilt/Koin 增加 APK 体积", 但 4.6 节 DolbyControlService 标注 `@AndroidEntryPoint` (Hilt 注解)**. 这是文档自相矛盾, 如果按 Hilt 实现就违反"轻量化"目标, 如果按手动 DI 就删除 Hilt 引用。|
| C-4 | M4 | **EqualizerViewModel.applyPresetCurve() 引用 `Preset.ROCK/POP/CLASSICAL/JAZZ/VOCAL/FLAT`, 但 Preset 枚举定义只有 `MOVIE/MUSIC/GAME/VOICE/CUSTOM`**. 这是枚举不存在的引用, 一编译就报错。|

### 3.2 High (11 条) — 应在 Phase 1 修复

| # | 模块 | 问题 |
|---|------|------|
| H-1 | M1 | `getevent -qlc 1` 依赖 PATH 中有 `getevent`, 但 KSU 环境下可能没有. 音量键选择超时后无明确退出码, 脚本可能挂起。|
| H-2 | M1 | `clean_old_artifacts` 删除 `daxapp/daxcontroller` 残留, 但这些目录名是其他模块的命名, 缺乏通用性。如果用户从其他 Dolby 模块迁移过来, 这个清理可能误删其文件。|
| H-3 | M1 | `perm_one` 对 `DolbyAtmosControl.apk` 使用 `u:object_r:system_file:s0` 上下文, 但 priv-app APK 通常需要 `privapp` 或 `apk_file` 等专用上下文类型, 可能导致 APK 无法被 PackageManager 加载。|
| H-4 | M1 | `action.sh` (M5) 的清理工作在 `service.sh` 中重复实现 (clean_probe), 两处分别清理, 维护时容易漏改一处。|
| H-5 | M2 | M2 文档说"保留 Qualcomm 音频 C2 default2 声明, 添加 Dolby C2 default1 实例", 但 VINTF manifest 中只声明了 `default1` 和 `default2`, 没有确保两者指向不同的服务名. 如果两者冲突, 框架会随机选一个。|
| H-6 | M3 | `merge_audio_effects` 的 awk 脚本假设 `<stream type="music">` 存在. 某些 OPlus ROM 的 audio_effects.xml 可能将效果应用到 `music, ring, alarm, system` 多个 stream, 脚本只处理 music 流会导致其他流缺少 DVL 监听器。|
| H-7 | M3 | `gsub` 只修正 3 个固定路径, 如果原 XML 中 `<library path="libswdap_sp.so">` 已经是正确的相对路径, gsub 不匹配也无害; 但如果原 XML 使用其他变体路径 (如 `libswdap_sp_v2.so`), 不会被修正, 合并后路径错误。|
| H-8 | M4 | `MainViewModel.init` 启动协程做 IO, ViewModel 重建时 (旋转屏幕) 会重复启动, 导致 `setEnabled` 被调用两次, 状态错乱。|
| H-9 | M4 | 文档说 `MODIFY_DEFAULT_AUDIO_EFFECTS` 是系统权限, 但没有提供获取 platform 签名的方法, 也没有 fallback 到普通应用签名的设计。|
| H-10 | M5 | 脚本头部 `set -e`, 任何命令失败立即退出, 但诊断工具应该**尽可能多收集信息**, 而不是遇到一个错误就停止. 后续采集 logcat、tombstone 等步骤可能因前一步失败而完全丢失。|
| H-11 | M6 | `detect_audio_stack` 通过文件存在判断 (`/vendor/lib64/libaconfig_storage_read_api_cc.so` 或 `/odm/etc/vintf/manifest/dvs-aidl-service.xml`), 但这些路径在某些 OPlus 系统中不存在或被裁剪. 误判 AIDL vs HIDL 会导致 `select_dap_control_path` 选择错误的策略, 引发音效问题。|

### 3.3 Medium (15 条) — 应在 Phase 2 前修复

| # | 模块 | 问题 |
|---|------|------|
| M-1 | M1 | `audio_stack_type` 写到 `$MODPATH/.audio_stack_type`, 但 service.sh 中没有读这个文件, 浪费 IO。|
| M-2 | M1 | `wait_prop` 在 `service.sh` 中调用多次, 每次 60s, 启动总耗时可能 > 5 分钟, 影响开机体验。|
| M-3 | M1 | SELinux 上下文 `u:object_r:system_file:s0` 在多处使用, 但具体文件 (如 `oplus.product.features_audiox.xml`) 在 system 分区, 应该是 `product_file` 或 `system_data_file` 而非 `system_file`。|
| M-4 | M2 | 文档测试方案 "AC-4 16ch CPU 占用 < 15%" 没有说测试机配置, 不可重复。|
| M-5 | M2 | 性能测试要求 "内存 < 100 MB", 没有说如何测量 (PSS? RSS? PSS+swap?).|
| M-6 | M2 | AC-3 和 E-AC-3 共用 `c2.dolby.eac3.decoder` 实例, 但 MIME type 不同. Codec2 框架通常要求一个 MediaCodec 实例对应一个 Type, 这里用单实例处理两种类型可能在某些 Android 版本上不被支持。|
| M-7 | M3 | 附录 A 中 `AudioEffect.EFFECT_TYPE_INSERT` 错误, DAP 是 post-process effect, 应该是 `EFFECT_TYPE_POST_PROCESSING` 或 `EFFECT_TYPE_NULL`。|
| M-8 | M3 | AudioX 效果移除硬编码 UUID `41f6c0f4-5d8f-11ec-bf63-0242ac130002`, 但 OPlus 后续版本可能换了 UUID, 需要在 M6 中维护已知 AudioX UUID 列表。|
| M-9 | M3 | 启动顺序: service.sh 启动 DMS 服务, 但 M4 APK 启动后立即 `bind()`. 如果 APK 启动比 service.sh 快, 会导致 `MAX_RETRIES=5` 全部失败。|
| M-10 | M4 | `EqualizerViewModel.applyPresetCurve()` 中 `else -> DEFAULT_GAINS` 的 else 分支永远不会被触发 (因为 `Preset.ROCK/POP/...` 不存在), 实际是 dead code。|
| M-11 | M4 | APK 没有 Crashlytics/ANR 检测集成, 现场崩溃后无法定位。|
| M-12 | M4 | `language_system` 在 strings.xml 是 "Follow System", 但 enum 是 `"system"`. 当用户选 "Follow System" 时, locale 标签 "system" 会被 `Locale.forLanguageTag("system")` 拒绝, 应该传空字符串。|
| M-13 | M5 | `TOMBSTONE_DIR` 变量名是 `DIR` 但实际存的是文件路径 (`head -1` 取了一个文件), 应该叫 `TOMBSTONE_FILE` 或用 `find -type d`。|
| M-14 | M5 | `/storage/emulated/0/dolbylog/` 永远不被清理, 多次运行后会积累大量日志, 占满用户存储。|
| M-15 | M6 | `get_device_dap_params` 返回 plain text, 调用方需要解析, 容易出错. 改为 `key=value` 形式更安全, 或返回 JSON。|

### 3.4 Low (8 条) — 可延后

| # | 模块 | 问题 |
|---|------|------|
| L-1 | M1 | `module.prop` 中 `author` 字段是团队名, 应该是具体维护者。|
| L-2 | M2 | 文档标题"基于 PJD110 设备提取, 适配 PKG110 (一加 ACE5)" 应该补充适配原理 (差异点)。|
| L-3 | M3 | UUID 常量表只在 M3 文档中, M4 又重复定义. 应该抽出到共享模块或文档。|
| L-4 | M4 | 没有 R8/ProGuard 规则, 实际打包时会有 warning。|
| L-5 | M4 | `DolbyControlService` 用 `@AndroidEntryPoint`, 但 onBind 返回 null — 实际上不需要 Hilt 注入, 文档自相矛盾。|
| L-6 | M5 | 日志格式 `[HH:MM:SS]` 与 M1 的 `[post-fs-data] HH:MM:SS` 不统一。|
| L-7 | M5 | 没有日志轮转策略, 旧报告不会自动清理。|
| L-8 | M6 | `is_oplus_device` 的 case 匹配中, 多次匹配 "oplus" 大小写不一致, 实际不会出问题但代码风格不统一。|

---

## 4. 模块专项发现

### 4.1 M1: KSU 模块打包框架

**职责评价:** ✓ 完整, 涵盖安装/卸载/服务启动全生命周期

**严重问题 (C-1, H-1 ~ H-4, M-1 ~ M-3):** 见上表

**一般问题:**
- `perm_recursive` 中 `set_perm_recursive` 后又 `chown`/`chmod` 一次, 重复 IO
- `wait_prop` 失败后 `exit 0`, 应该根据错误码决定是否继续
- `customize.sh` 没有 `set -u` 防未定义变量
- 卸载脚本 `uninstall.sh` 不处理 KSU/Magisk 调用差异 (Magisk 卸载时 MODDIR 可能不存在)
- `post-fs-data.sh` 的 `setup_diag_dir` 创建 `/storage/emulated/0/dolbylog` 在 post-fs-data 阶段, 此时 sdcardfs 可能未挂载, 会失败

**测试方案缺陷:**
- 单元测试需要真实 KSU/Magisk 环境, 无法在 CI 中跑
- 集成测试 "在 KSU Manager 中安装并重启" 是黑盒, 无法定位失败原因
- 冒烟测试 "无 ANR/崩溃" 时间 5 分钟太短, 应该 1 小时+

### 4.2 M2: 杜比解码器

**职责评价:** ✓ 清晰, 4 种格式覆盖完整

**严重问题 (C-2, H-5, M-4 ~ M-6):** 见上表

**一般问题:**
- 没有 VINTF 兼容性矩阵 (VINTF 6.0 vs 7.0 vs 8.0 兼容性)
- 启动参数 "无需额外参数" 但不同 OPlus 版本可能需要不同 vendor 路径
- 没有 `c2.dolby.eac3.decoder` 与 `OMX.dolby.eac3.decoder` alias 的实际验证步骤
- 文档提到的 `dumpsys media.codec2` 只能列出 Codec2 服务, 不能直接验证解码器是否能成功解码

**测试方案缺陷:**
- 性能测试基线 (CPU 15% / 内存 100MB) 没有说 "什么 SoC + 什么 Android 版本"
- "在 VLC 中打开 AC-4 编码的 MKV 文件" 是黑盒, 失败原因可能是 VLC 不识别, 不一定是解码器

### 4.3 M3: 杜比音效链

**职责评价:** ⚠ 关键路径, 文档篇幅最长, 但也是问题最多的模块

**严重问题 (C-1 shared, H-6, H-7, M-7 ~ M-9):** 见上表

**一般问题:**
- 主方案 vs 备选方案的判定条件模糊, 文档没说 "什么 logs 表示切到备选"
- `audio_effects.xml` 静态模板定义了 `<postprocess>`, 但 `merge_audio_effects` 会修改这一段, 模板上的 postprocess 实际是 fallback, 文档没说
- `oplus.product.features_audiox.xml` 放在 `my_product/etc/permissions/`, 但 `mount --bind` 在 service.sh 中, 启动时序没说 (boot_completed 之前还是之后)
- DAP UUID 来自参考模块的 .so, 文档没说 "如果 UUID 不匹配如何检测"
- `vendor.dolby_sp.hardware.dmssp@2.0.so` 标注 "HIDL 兼容保留", 但 Android 16 不再用 HIDL, 保留可能引入新问题

**测试方案缺陷:**
- 单元测试 `merge_audio_effects` 的 mock XML 没有覆盖所有 OPlus 变体
- 异常测试 "DMS 服务崩溃恢复" 没说恢复时间要求
- 端到端测试 "连续播放音乐 1 小时" 没有指定文件/采样率

### 4.4 M4: 配置 APK

**职责评价:** ✗ 设计文档中存在多处**自相矛盾**, 需要重大返工

**严重问题 (C-3, C-4, H-8, H-9, M-7, M-10, M-12):** 见上表

**一般问题:**
- `i18n` 文档说"中英文切换", 但 enum `AppLanguage` 有 3 个值 (system/zh/en), 实现时需要重启 Activity 才能生效, 文档没提
- 状态机图 7.1 "DAP 效果状态机" 中 `DISABLING` 状态的转换箭头不闭合
- 测试方案说 "使用 MockK 模拟 AudioEffect", 但 `AudioEffect` 是 final class, MockK 模拟 final 需要 `mockk-android` 或 `mockkAgent`, 文档没提
- 整个 APK 的 ProGuard 规则缺失
- Material 3 主题在 Android 14 (SDK 34) 上需要额外的 Material 3 库依赖, 文档没提
- `BootReceiver` 触发前台服务, Android 14+ 对后台启动服务有限制, 文档没提
- `AudioDeviceMonitor` 实现细节缺失 (监听 AudioManager 哪个 callback?)

**测试方案缺陷:**
- 单元测试 10.1 中 "LanguageSelector 切换" 需要模拟用户点击, 实际需要 instrumented test
- "内存压力" 测试依赖 LeakCanary, 但 LeakCanary 是 debug-only 依赖, release 包不会有
- UI 测试 "横竖屏切换" 需要 Espresso, 文档没提

### 4.5 M5: 诊断工具

**职责评价:** ✓ 实用, 10 阶段流程覆盖完整

**严重问题 (H-10, M-13, M-14, L-6, L-7):** 见上表

**一般问题:**
- `set -e` + `grep` 组合陷阱: `grep` 找不到匹配返回 1, 会触发 `set -e` 退出. 例如 `grep -l ... /data/tombstones/tombstone_*` 在空目录上立即失败
- `find /data/anr -newer ...` 需要 `find` 支持 `-newer`, 但 toybox find 与 GNU find 行为略有差异
- `dumpsys media.audio_flinger` 输出可能达数 MB, 写入 `$DUMP_FILE` 时没有大小限制
- 没有去重逻辑, 同一问题在多个 logcat 行中重复
- 输出路径 `/storage/emulated/0/dolbylog/` 在 scoped storage (Android 11+) 上可能需要权限

**测试方案缺陷:**
- "在无 root 权限运行" 的失败行为没说 (应该明确提示)
- 报告大小没有约束, 大量 logcat 输出可能让用户无法分享
- 没有 "诊断快速模式" vs "完整模式" 选项

### 4.6 M6: 兼容性适配

**职责评价:** ✓ 全面, 覆盖 6 大类检测

**严重问题 (C-1 shared, H-11, M-15):** 见上表

**一般问题:**
- `apply_sepolicy_patches` 只能注入 magiskpolicy, 但 KSU 环境下没有 magiskpolicy
- SELinux 规则 `vendor_dolby_dms_service` 这个 type 名称是猜测的, 实际 .service 进程的 type 可能叫 `vendor_init` 或 `vendor_app`
- `get_device_dap_params` 没有验证 `bt_max_sampling_rate=96000` 在 PKG120 上是否真的支持
- 多机型适配矩阵中 "一加 ACE5 Pro" 标 "待确认" 多处, 这些"待确认"应该转成 issue 跟踪
- `pre_install_check` 返回 `$errors`, 但调用方 (M1 customize.sh) 不检查返回值, 实际不会 abort

**测试方案缺陷:**
- 集成测试 "在 KSU/Magisk/APatch 设备上安装" 需要 3 套测试环境, CI 成本高
- "非 OPlus 设备" 没有具体测试机列表
- 多机型测试只有 PKG110, PKG120 没列

---

## 5. 横向问题 (跨模块)

### 5.1 单一事实源缺失

| 常量 | 出现位置 | 实际值 | 风险 |
|------|----------|--------|------|
| DAP UUID `9d4921da-8225-4f29-aefa-39537a04bcaa` | M3 (XML, awk), M4 (Kotlin), M5 (grep), M6 (grep) | 4 处重复 | 任何一处修改, 必须同步 3 处 |
| `DMS_SERVICE_NAME = "vendor.dolby.dms.IDms/default"` | M3 (路径), M4 (Kotlin 常量) | 2 处 | 同上 |
| C2 服务名 `vendor.dolby_sp.media.c2@1.0-service` | M1 (customize.sh, service.sh), M2 (VINTF), M5 (action.sh) | 4 处 | 同上 |
| `multimedia_dolby_dax_dflt.xml` 路径 | M3 (文件清单), M1 (perm_one), M5 (file_exists) | 3 处 | 同上 |
| `9d4921da-8225-4f29-aefa-39537a04bcaa` 字符串长度 | M3 静态模板, M3 awk 验证, M4 DapEffectController, M5 grep | 4 处 | grep 写错 1 字符就漏检 |

**建议:** 抽出 `constants.sh` / `Constants.kt`, 集中管理; 构建时校验一致性。

### 5.2 缺失的横切关注点

| 关注点 | 当前状态 | 风险 |
|--------|----------|------|
| 构建系统 | ❌ 无 | Phase 4 集成时无法组装 ZIP |
| CI/CD | ❌ 无 | 每次修改都需要手动验证 |
| 版本管理 | 半成品 (`versionCode=100` 硬编码) | 升级策略缺失 |
| 更新机制 | ❌ 无 | 用户如何升级模块? |
| 国际化 (UI 文案) | ✓ M4 完整 | OK |
| 国际化 (错误信息) | 半成品 (M1 `ui_print` 是中文硬编码) | 安装日志全英文 vs 全中文不一致 |
| 可访问性 (a11y) | ❌ 无 | 视障用户无法使用 |
| 错误码体系 | ❌ 无 | 跨模块错误信息无法关联 |
| 单一事实源 (UUID/路径) | ❌ 无 | 见 5.1 |
| 回滚机制 | ❌ 无 | SELinux 补丁怎么撤回? |
| 安全审计 | ❌ 无 | 谁审查了 SELinux 规则的安全性? |

### 5.3 文档缺口

- 无故障排查手册 (FAQ)
- 无安装手册 (面向终端用户)
- 无开发贡献指南
- 无 changelog 模板
- 无 security policy
- 无 license (M1 description 提到 "Open source project" 但没说用什么 license)

### 5.4 测试盲点矩阵

| 测试类型 | M1 | M2 | M3 | M4 | M5 | M6 |
|----------|----|----|----|----|----|-----|
| Shell `bats` 单元测试 | ✓ (需真机) | n/a | ✓ (需真机) | n/a | ✓ (需真机) | ✓ (需真机) |
| Kotlin JUnit 单元测试 | n/a | n/a | n/a | ✗ (MockK 配置缺失) | n/a | n/a |
| Kotlin UI 测试 | n/a | n/a | n/a | ✗ (Espresso 配置缺失) | n/a | n/a |
| XML 静态校验 | n/a | ✓ | ✓ | n/a | n/a | n/a |
| 集成测试 (真机) | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ |
| CI 流水线 | ✗ | ✗ | ✗ | ✗ | ✗ | ✗ |
| 回归测试 | ✗ | ✗ | ✗ | ✗ | ✗ | ✗ |
| 性能测试 | ✗ | ✓ (基线缺失) | ✗ | ✗ | ✗ | ✗ |
| 异常测试 | ✓ (浅) | ✓ (浅) | ✓ (浅) | ✓ (浅) | ✓ (浅) | ✓ (浅) |

**关键发现:** 86% 的测试需要真机, CI 无法自动化. M4 的 Kotlin 单元测试框架 (MockK) 引入但未实际使用.

---

## 6. 改进建议 (按优先级)

### P0 — 立即修复 (Phase 0, 1-2 天)

1. **解决 C-1**: 在 M1/M6 中二选一保留 `detect_root_solution`, 删除另一处. 推荐保留 M1 的版本 (有更详细的输出).
2. **解决 C-3 / C-4 (M4 自相矛盾)**: 重新审视 M4 的依赖注入策略, 决定 Hilt vs 手动 DI, 修改 4.6 节. 同时修复 `Preset.ROCK/POP/...` 引用错误.
3. **解决 C-2 (SHA256 待确认)**: 立即获取参考模块的 .so 文件, 计算 SHA256, 写入 `M2/checksums.txt` 模板. 此项**阻塞** Phase 4 集成测试.
4. **建立 `constants.sh`**: 抽出所有跨模块 UUID/路径/服务名, 作为单一事实源.

### P1 — Phase 1 内修复 (1 周内)

5. 修复 H-1 (音量键 getevent 兼容性) — 用 fallback 等待.
6. 修复 H-3 (APK SELinux 上下文) — 使用 `privapp` 类型.
7. 修复 H-6 / H-7 (M3 awk 脚本) — 重写为 Python 脚本或更鲁棒的 awk.
8. 修复 H-8 (M4 ViewModel 重复协程) — 使用 `viewModelScope.launchWhenStarted` 或类似机制.
9. 修复 H-10 (M5 `set -e`) — 改为 `set +e` + 显式错误处理.
10. 修复 H-11 (M6 音频栈检测) — 增加多种检测方法, 取并集判断.
11. 增加 M3 的 "DAP UUID 自检" 步骤, 在 install.sh 中验证 .so 实际支持的 UUID.

### P2 — 实施过程中修复

12. 修复 M-2 (M1 wait_prop 超时过长) — 减少到 30s.
13. 修复 M-7 (附录 A EFFECT_TYPE) — 改用 EFFECT_TYPE_POST_PROCESSING.
14. 修复 M-12 (M4 语言 code) — `AppLanguage.SYSTEM.code` 改为 `""`.
15. 修复 M-13 (M5 变量名) — `TOMBSTONE_DIR` → `TOMBSTONE_FILE`.
16. 修复 M-14 (M5 日志清理) — 增加保留最近 10 份报告的清理逻辑.
17. 修复 M-15 (M6 get_device_dap_params) — 返回 key=value 形式.

### P3 — 后续版本

18. 建立构建系统 (建议: `make` + shell script).
19. 引入 CI (建议: GitHub Actions + `bats` runner).
20. 完善 M4 测试 (MockK + Turbine + Espresso).
21. 添加 LICENSE 文件.
22. 添加 README 故障排查手册.
23. 优化日志格式统一.

---

## 7. 实施路线图建议

基于审查发现, 建议调整原 progress.md 的 4 阶段计划:

### 修订版 Phase 0: 文档定稿 (1-2 天)
- 解决 P0 的 4 个 Critical 问题
- 建立 `constants.sh` 单一事实源
- 锁定 SHA256 (如可能)

### 修订版 Phase 1: 基础设施 (1 周)
- M1 + M6 + M2 实施
- 同时建立 `build.sh` 整合脚本
- 同时建立 `tests/` 目录和 `bats` 测试套件
- P1 修复

### 修订版 Phase 2: 核心功能 (1-2 周)
- M3 + M5 实施
- 重点: M3 的 `merge_audio_effects` 必须**先用真机 audio_effects.xml 测试** (准备至少 3 个不同 OPlus 版本的样例)
- P2 修复

### 修订版 Phase 3: 用户界面 (1-2 周)
- M4 实施
- 重点: 解决 C-3/C-4 后再开始
- 准备 MockK 测试

### 修订版 Phase 4: 集成 (1 周)
- ZIP 组装
- 完整集成测试
- 回归测试

### 修订版 Phase 5: 发布 (持续)
- CI 流水线
- 文档完善 (README/FAQ)
- 社区反馈循环

---

## 8. 附录

### 8.1 重复 UUID/路径/常量清单 (节选)

```bash
# constants.sh (建议新增文件)
DAP_UUID="9d4921da-8225-4f29-aefa-39537a04bcaa"
DVL_MUSIC_UUID="40f66c8b-5aa5-4345-8919-53ec431aaa98"
DVL_RING_UUID="21d14087-558a-4f21-94a9-5002dce64bce"
DVL_ALARM_UUID="6aff229c-30c6-4cc8-9957-dbfe5c1bd7f6"
DVL_SYSTEM_UUID="874db4d8-051d-4b7b-bd95-a3bebc837e9e"
DVL_NOTIFICATION_UUID="1f0091e3-6ad8-40fe-9b09-5948f9a26e7e"
GAMEDAP_UUID="3783c334-d3a0-4d13-874f-0032e5fb80e2"
DMS_SERVICE="vendor.dolby.dms.IDms/default"
C2_SERVICE="vendor.dolby_sp.media.c2@1.0-service"
C2_BIN="/vendor/bin/hw/$C2_SERVICE"
DMS_BIN="/vendor/bin/hw/vendor.dolby.dms.service"
MODULE_ID="dolby_atmos_oplus_folk"
MODULE_VERSION="v1.0.0"
MODULE_VERSION_CODE=100
```

### 8.2 待确认的 SHA256 列表

| 文件 | 状态 |
|------|------|
| `libcodec2_soft_ac4dec_sp.so` | ❌ 待确认 (阻塞) |
| `libcodec2_soft_ddpdec_sp.so` | ❌ 待确认 (阻塞) |
| `libcodec2_soft_common.so` | ❌ 待确认 (阻塞) |
| `libcodec2_store_dolby_sp.so` | ❌ 待确认 (阻塞) |
| `libdeccfg_sp.so` | ❌ 待确认 (阻塞) |
| `libYokiScale.so` | ❌ 待确认 (阻塞) |
| `libdlbdsservice_sp.so` | ❌ 待确认 |
| `libdapparamstorage_sp.so` | ❌ 待确认 |
| `libdlbpreg_sp.so` | ❌ 待确认 |
| `libdmshal.so` | ❌ 待确认 |
| `libspatializerparamstorage.so` | ❌ 待确认 |
| `vendor.dolby.dms-V1-ndk.so` | ❌ 待确认 |
| `vendor.dolby_sp.hardware.dmssp@2.0.so` | ❌ 待确认 |
| `vendor.dolby.dms.service` | ❌ 待确认 |
| `vendor.dolby_sp.media.c2@1.0-service` | ❌ 待确认 |
| `libswdap_sp.so` | ❌ 待确认 |
| `libdlbvol_sp.so` | ❌ 待确认 |
| `libswgamedap_sp.so` | ❌ 待确认 |

**共 18 个二进制文件 SHA256 待确认, 建议在 Phase 0 完成。**

### 8.3 测试盲点详细列表

| 场景 | 模块 | 风险 |
|------|------|------|
| Android 17 (未来) 兼容性 | 全部 | 文档只测到 Android 16 |
| 非 ACE5 设备 (如一加 12) | M3, M6 | "一加 ACE5" 命名暗示只支持 ACE5 |
| KSU 11000 以下版本 | M6 | KSU_VER_CODE 检查阈值 11000 是猜测 |
| 蓝牙 LDAC 编码 | M4 | 蓝牙设备独立配置提到但无测试 |
| USB-C 耳机 DAC 切换 | M4 | 设备类型枚举包含但无处理 |
| 通话中 (voice call) | M3 | 通话流是否应用 DAP? |
| 多用户 (Android 多用户) | M4 | 跨用户的音频配置 |
| 折叠屏 | M4 | 屏幕旋转/折叠的 UI 行为 |
| 横屏游戏 | M3, M4 | 横屏时音效配置是否一致 |
| 直播类应用 (如 Twitch) | M2, M3 | 实时流的解码+音效 |
| 系统升级后 | 全部 | OTA 升级后模块兼容性 |
| Magisk Hide / DenyList | M1, M4 | 被 Magisk 隐藏时如何自启 |
| Work Profile | M4 | 工作资料中的应用能否使用 |
| 内存不足 (low memory) | 全部 | OOM 时模块是否优雅失败 |

---

## 9. 审查结论

**文档状态:** 设计阶段基本完成, 但**实现细节仍需大量补全**.

**主要风险:**
1. M4 设计文档自相矛盾 (C-3, C-4), 需要返工
2. 18 个二进制 SHA256 未确认, 阻塞集成测试
3. 跨模块 UUID/路径无单一事实源, 维护风险高
4. 86% 测试需真机, CI 无法自动化

**建议:**
1. 在编码前先解决 P0 的 4 个 Critical 问题
2. 优先建立 `constants.sh` 单一事实源
3. 重新审视 M4 设计, 解决自相矛盾
4. 获取参考模块二进制, 计算 SHA256
5. 实施时按修订版 5 阶段路线图推进

**不要:**
- 直接按原 progress.md 4 阶段实施, 风险过高
- 跳过 P0 直接进入 Phase 1
- 在没有 constants.sh 的情况下开始编码

---

> **审查结束. 报告版本:** v1.0
> **下一步:** 等待用户决定是否针对某个模块进行更深入的审查, 或开始 P0 修复.
